# checkpoint — DDL2-Batch4 (Worfa), session 2026-09-10

Status (04:55 Istanbul, 11 Sep): steps 0–7 payloads DONE, **NOTHING PUSHED YET**. Push blocked at every retry since 02:15 (02:50, 03:52, 04:53): the sandbox egress proxy rejects CONNECT to ymjviw-rz.myshopify.com, cdn.shopify.com and api.dataforseo.com (403 — organization policy / allowlist not active). Hourly retries continue automatically; this doc is rewritten only when something changes. The user should check the network-egress allowlist.
Ready on disk in /home/claude/work: final/d00–d49.json (gate CLEAN over all 50, 6c corrections + independent CTA review applied), rewrites.json, push/mut0–4.json (aliased productUpdate: title, descriptionHtml, seo whole, productType, tags = snapshot ∪ season, category gid, collectionsToJoin), push/cta0–1.json (metafieldsSet custom.cta_benefits), push/alt0–9.json (472 fileUpdate alts). backup-titles doc in project; backup-alt + kw-cache sent to chat. Two collections already created live: "Women's Fashion", "Gifts & Party".
Resume procedure: (1) `python3 shopify_api.py fetch DDL2-Batch4 live_pre.json` and compare live titles with titles_final.json — if live already carries the NEW titles, the push happened: skip to step 8. (2) Otherwise `for f in push/mut*.json push/cta*.json push/alt*.json; do python3 shopify_api.py mutate $f; done`. (3) Step 8: fetch live_after.json, `python3 verify.py products.json live_after.json collections.json`, `python3 head_check.py live_after.json`, write claude/run-log-ddl2-batch4.md.
If the work dir is gone (new container): the batch must be re-run from step 0 — but first fetch live and check titles against claude/backup-titles-ddl2-batch4.md so a pushed batch is never re-processed.
